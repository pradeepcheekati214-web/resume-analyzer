[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --tb=short
    --strict-markers
    -q
markers =
    unit: Pure unit tests (no I/O)
    integration: Tests that hit real AWS services (skipped in CI)
